import express from 'express'

const paymentRoute = express.Router()

// paymentRoute.post(
//   "/payments/:hostelId/payment",
//   bookingContrller.createBooking.bind(bookingContrller)
// );

export default paymentRoute